// Command dashboard starts the katamaran web dashboard. The command stays
// intentionally thin and delegates flag parsing, HTTP setup, and orchestration
// wiring to internal/dashboard so those paths can be exercised by tests.
package main
